@model Capitol_Theatre.Models.CreateUserViewModel

@{
    ViewData["Title"] = "Create New User";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="my-4">➕ Create New User</h2>

<form asp-action="CreateUser" method="post">
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="mb-3">
        <label asp-for="Email" class="form-label"></label>
        <input asp-for="Email" class="form-control" />
        <span asp-validation-for="Email" class="text-danger"></span>
    </div>
    <div class="alert alert-info">
        <strong>Password Requirements:</strong>
        <ul class="mb-0">
            <li>At least <strong>6 characters</strong> in length</li>
            <li>Must contain at least one <strong>uppercase letter</strong></li>
            <li>Must contain at least one <strong>lowercase letter</strong></li>
            <li>Must contain at least one <strong>digit</strong></li>
            <li>Must include at least one <strong>non-alphanumeric character</strong> (e.g., <code>!</code>, <code>#</code>)</li>
        </ul>
    </div>
    <div class="mb-3">
        <label asp-for="Password" class="form-label"></label>
        <input asp-for="Password" class="form-control" type="password" />
        <span asp-validation-for="Password" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="ConfirmPassword" class="form-label"></label>
        <input asp-for="ConfirmPassword" class="form-control" type="password" />
        <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-success">Create User</button>
    <a asp-action="Users" class="btn btn-secondary">Cancel</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
